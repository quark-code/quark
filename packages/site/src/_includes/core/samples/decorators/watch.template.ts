(function () {
    const { html } = require('@quark-elements/doc');

    const render = (data) => {
        return html`
        <div>Watch Decorator Sample</div>
    `;
    };

    module.exports = {
        render: render,
    };
})();
